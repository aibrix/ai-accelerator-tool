package utils

// GPU_CARD_COUNT is the environment variable for the number of GPU cards.
const GPU_CARD_COUNT = "GPU_CARD_COUNT"

// GPU_MOCK_DIR is the directory for the GPU mock environment.
const GPU_MOCK_DIR = "/opt/gpu_mock"
